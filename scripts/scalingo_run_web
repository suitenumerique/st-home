#!/bin/bash

# Scalingo does not support empty env vars, so we use "-" as a placeholder meaning "disabled".
# During an incident, replace "-" with actual values (e.g. "1.2.3.4" or "FR").
DISABLED="-"

# Generate blocklist snippet from $BLOCKLIST (comma-separated IPs/CIDRs, e.g. "1.2.3.4,5.6.7.0/24")
if [ -n "$BLOCKLIST" ] && [ "$BLOCKLIST" != "$DISABLED" ]; then
  BLOCKLIST_SPACED=$(echo "$BLOCKLIST" | tr ',' ' ')
  cat > blocklist.caddy <<EOF
@blocked client_ip $BLOCKLIST_SPACED
respond @blocked "Access denied" 403
EOF
else
  : > blocklist.caddy
fi

# Generate geo-blocking snippet from $GEO_ALLOWED_COUNTRIES (comma-separated country codes, e.g. "FR,DE")
if [ -n "$GEO_ALLOWED_COUNTRIES" ] && [ "$GEO_ALLOWED_COUNTRIES" != "$DISABLED" ]; then
  GEO_SPACED=$(echo "$GEO_ALLOWED_COUNTRIES" | tr ',' ' ')
  cat > geoblocking.caddy <<EOF
@geoblocked not {
	maxmind_geolocation {
		db_path ./dumps/geoip-country.mmdb
		allow_countries $GEO_SPACED
	}
}
respond @geoblocked "Access denied" 403
EOF
else
  : > geoblocking.caddy
fi

# Start the NextJS server
npm run start &

# Start the Caddy server
bin/caddy run --config Caddyfile &

# if the current shell is killed, also terminate all its children
trap "pkill SIGTERM -P $$" SIGTERM

# wait for a single child to finish,
wait -n
# then kill all the other tasks
pkill -P $$
