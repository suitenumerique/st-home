{
  "jobs": [
    {
      "command": "20 */4 * * * bash -c 'python -m tasks.sync && node -r @swc-node/register scripts/db-seed.ts dumps'",
      "size": "2XL"
    },
    {
      "command": "0 2 * * * bash -c 'python -c \"from tasks.check_website import queue_all; queue_all()\" && python -c \"from tasks.check_dns import queue_all; queue_all()\"'",
      "size": "M"
    },
    {
      "command": "0 0 1 * * python -m tasks.historize",
      "size": "M"
    },
    {
      "command": "0 */1 * * * make cms-refresh"
    }
  ]
}
